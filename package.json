{
  "name": "request-store",
  "version": "0.0.3",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf lib && bmp src -l typescript",
    "release": "yarn run build && standard-version && npm publish --registry=https://registry.npmjs.org/"
  },
  "dependencies": {
    "tslib": "^1.9.3"
  },
  "devDependencies": {
    "@types/node": "^10.12.18",
    "build-my-package": "^0.0.16",
    "isomorphic-fetch": "^2.2.1",
    "standard-version": "^4.4.0",
    "ts-lint": "^4.5.1",
    "tslint-config-prettier": "^1.17.0",
    "typescript": "^3.2.2"
  }
}
